/* =========================================================
   REVIVE ORGANISATION — CURRENT EVENT / ANNOUNCEMENT BANNER
   The one file to edit whenever there's a workshop, camp, or
   announcement to run on the site-wide scrolling banner.

   Set active:true and fill in text + eventPageUrl to turn the
   banner on across every page. Set active:false to hide it
   completely — nothing else needs to change.

   text          — short line shown in the scrolling banner,
                   e.g. "MEDICAL CAMP · KORANGI · SAT 15 MARCH — TAP FOR DETAILS"
   eventPageUrl  — where clicking the banner sends people,
                   normally /happening/
   ========================================================= */
window.CURRENT_EVENT = {
  active: false,
  text: "",
  eventPageUrl: "/happening/"
};
